{
	"name": "root",
	"private": true,
	"devDependencies": {
		"lerna": "^4.0.0"
	},
	"scripts": {
		"start":"lerna run start",
		"test": "lerna run test --scope={web-client,server-api}", 
		"new-version":"lerna version --conventional-commit --yes",
		"diff":"lerna diff"
	}
	
}
